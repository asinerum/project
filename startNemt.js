////////////////////////////////////////////////////////////
getMetamask(function(){startNemt();promoteMenu();dbh('pro_id','id',menu.onDefiProgReId)});
txgwei=0;
////////////////////////////////////////////////////////////